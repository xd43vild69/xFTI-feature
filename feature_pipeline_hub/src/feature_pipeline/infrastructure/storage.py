"""Local filesystem management: raw ingestion scanning and processed-dataset caching.

Placeholder for Iteración 2 (UI Base - Galería e Ingesta). Will handle
scanning `data/raw/` for image+caption pairs and writing curated output
to `data/processed/`.
"""


def scan_raw_folder(folder_path: str) -> list[str]:
    """List image file paths found under a raw ingestion folder."""
    raise NotImplementedError("Implemented in Iteración 2")
